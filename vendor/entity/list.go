// NOTE: DROP

package entity

// Golang seems to have not generic for interface now(1.9) .....

/*
type List interface {
	// NOTE: Need a in-place Deserialize ?
	// Deserialize(Decoder) error

	Size() int

	// FIXME: how to express Ref ?
	// Ref(util.Identifier) interface{}
	Contains(util.Identifier) bool

	ForEach(interface{}) error
}
*/
